<!DOCTYPE html>
<html>
<head>
  <meta charset=utf-8 />
  <title>Gluent<% if @page_title %><%=h " | " + @page_title %><% end %></title>
  <%= stylesheets %>
  <%= javascripts %>
</head>
<body>
  <nav id="navi-bar">
    <a href="/">TOP</a>
    <a href="/logout">logout</a>
    <form action="/search" method="get" role="search">
      <input type="input" name="query" value="<%= h @query %>" />
      <button type="submit">search</button>
    </form>
  </nav>

  <div class="row">
<%= yield %>
  </div>
</body>
</html>
